(()=>{const A=window.KVA;if(!A)return;const{lev,city,name,D,I,esc,pg,set,head,line,draw}=A;for(let n=14;n<=17;n++){let d=n-13;if(lev==='young')set(n,`${head('DAY '+d,'My picture diary','✏️')}<p class="label">MY DAY FELT LIKE…</p><div class="mood-row">😄 🤩 😴 😮 😋</div><p class="label">MY FAVOURITE THING WAS…</p>${line()}${draw('DRAW MY BEST MOMENT FROM DAY '+d,'')}<p class="label">ONE WORD FOR TODAY</p>${line()}`);else if(lev==='older')set(n,`${head('DAY '+d,'Explorer field journal','✏️')}<p class="label">MOST IMPORTANT OBSERVATION</p>${line()}${line()}<p class="label">A FACT OR IDEA I CAN VERIFY</p>${line()}${line()}<p class="label">HOW TODAY CHANGED MY VIEW OF ${esc(city.toUpperCase())}</p>${line()}${line()}<p class="label">A QUESTION I STILL HAVE</p>${line()}${draw('QUICK FIELD SKETCH — DAY '+d)}`)}let p18=pg(18);if(p18&&lev==='young'){let m=p18.querySelector('.muted'),d=p18.querySelector('.draw');if(m)m.textContent=`Draw your favourite part of ${city}. Big, colourful and silly is allowed!`;if(d)d.innerHTML=`MY ${esc(city.toUpperCase())} PICTURE<br><br>Bonus: hide a tiny ${I[0]} somewhere.`}else if(p18&&lev==='older'){let m=p18.querySelector('.muted'),d=p18.querySelector('.draw');if(m)m.textContent=`Reconstruct one ${city} scene from memory. Focus on proportion, foreground/background and one accurate local detail.`;if(d)d.innerHTML=`FIELD SKETCH OF ${esc(city.toUpperCase())}<br><br>Label 3 details that make the scene identifiable.`}if(lev==='young')set(19,`${head('SEND A MEMORY','My tiny postcard','💌')}<div class="card soft"><p>Dear ____________________,</p><p>I am in <strong>${esc(city)}</strong>!</p><p>I saw __________________________.</p><p>I ate / spotted __________________.</p><p>My favourite thing was ____________.</p><p>From, <strong>${esc(name)}</strong></p></div>${draw('DRAW THE FRONT OF YOUR POSTCARD','')}`);else if(lev==='older')set(19,`${head('SEND A MEMORY','Write a real travel postcard','💌')}<div class="card soft"><p>Write 5–7 sentences to someone who has never visited ${esc(city)}.</p><p>Include: one sensory detail • one surprising fact • one recommendation • one thing that challenged your expectations.</p>${line()}${line()}${line()}${line()}${line()}${line()}</div><div class="card mint"><strong>Editing challenge:</strong> replace one vague adjective with a precise description.</div>`);if(lev==='young')set(20,`${head('WOW!','Three city facts','🤯')}<div class="facts">${(D.facts||[]).map((x,i)=>`<div class="fact"><b>${['⭐','🌟','✨'][i]}</b>${esc(x)}</div>`).join('')}</div><p class="label">CIRCLE YOUR FAVOURITE FACT: 1 • 2 • 3</p>${draw('DRAW WHAT YOU THINK THAT FACT LOOKS LIKE')}`);else if(lev==='older')set(20,`${head('SOURCE CHECK','Facts & questions','🧠')}<div class="facts">${(D.facts||[]).map((x,i)=>`<div class="fact"><b>0${i+1}</b>${esc(x)}</div>`).join('')}</div><div class="card soft"><h3>Verify one fact</h3><p>Find a sign, museum label, guide, official source or knowledgeable adult that adds context.</p>${line()}${line()}</div><div class="card mint"><h3>Build a better question</h3><p>Turn one fact into a “why” or “how” question.</p>${line()}</div>`)})();