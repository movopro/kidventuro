{
  "name": "kidventuro-api",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "test": "node test-webhook.mjs && node test-ai.mjs && node ../test-catalog.mjs && node ../test-checkout-safety.mjs && node ../test-launch.mjs",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
