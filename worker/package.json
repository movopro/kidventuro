{
  "name": "kidventuro-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node test-webhook.mjs",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
