{
  "name": "kidventuro-api",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "test": "node test-webhook.mjs && node test-ai.mjs && node test-analytics.mjs && node ../test-catalog.mjs && node ../test-checkout-safety.mjs && node ../test-launch.mjs && node ../test-seo-localization.mjs",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
